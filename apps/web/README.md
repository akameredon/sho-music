# Sho Music Web

Next.js listener + creator frontend for Sho Music 2.0.

```bash
pnpm --filter @sho-music/web dev
# → http://localhost:3001
```

Proxies `/api/*` to the NestJS API (`localhost:3000/v1`).
